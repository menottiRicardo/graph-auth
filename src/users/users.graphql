scalar DateTime

type User {
  # Example field (placeholder)
  id: Int!
  username: String!
  createdAt: DateTime
}

input CreateUserInput {
  # Example field (placeholder)
  exampleField: Int
}

type Query {
  users: [User]!
  user(id: Int!): User
}

type Mutation {
  createUser(createUserInput: CreateUserInput!): User!
}
